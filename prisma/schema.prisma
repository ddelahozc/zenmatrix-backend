// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

// Looking for ways to speed up your queries, or scale easily with your serverless or edge functions?
// Try Prisma Accelerate: https://pris.ly/cli/accelerate-init
// output   = "../generated/prisma"  --- Esta linea se encontraba en generator client


generator client {
   provider = "prisma-client-js"
}

datasource db {
  provider = "mysql"
  url      = env("DATABASE_URL")
}


model Task {
  id             Int       @id @default(autoincrement())
  proyecto       String
  responsable    String
  titulo         String
  descripcion    String?
  fechaInicio    DateTime  @default(now())
  fechaVencimiento DateTime?
  fechaTerminada DateTime?
  prioridad      String
  isCompleted    Boolean   @default(false)
  createdAt      DateTime  @default(now())
  updatedAt      DateTime  @updatedAt
  // Enlazar tarea a un usuario, y la relación ahora es REQUERIDA
  userId         Int       // <<-- ¡Aquí es Int, NO Int?!
  user           User      @relation("UserTasks", fields: [userId], references: [id]) // <<-- ¡Aquí es User, NO User?!
}

// Modelo de Usuario
model User {
  id        Int      @id @default(autoincrement())
  email     String   @unique
  password  String
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  tasks     Task[]   @relation("UserTasks")
}